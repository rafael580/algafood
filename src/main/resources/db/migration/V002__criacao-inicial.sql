 CREATE TABLE `cidade`(
  `id`      BIGINT          NOT NULL    AUTO_INCREMENT,
    `nome_cidade`    VARCHAR(50)     NOT NULL,
  	`nome_estado`    VARCHAR(50)     NOT NULL,

     PRIMARY KEY (`id`)
  );
